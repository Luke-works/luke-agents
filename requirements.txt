fastapi==0.116.2
uvicorn[standard]==0.34.0
pydantic==2.10.4
groq==1.5.0
openai>=1.99.0            # OpenAI brain (GPT-5 nano); only used when OPENAI_API_KEY is set
python-dotenv==1.2.1
psycopg2-binary==2.9.10   # transcript store (only used when DATABASE_URL is set)
redis==5.2.1              # shared rate-limit store (only used when REDIS_URL is set; #27)
# Optional alternative brains:
# google-genai==0.8.0   # Gemini (blocked for managed domains)
# ollama==0.4.5         # local model for dev (not usable on Render free tier)
